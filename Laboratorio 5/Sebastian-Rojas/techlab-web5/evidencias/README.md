# Evidencias — Semana 5

Coloca aquí tus capturas de pantalla, nombradas por caso (por ejemplo `CP-03.png`):

- CP-01: salida de `java --version`.
- CP-02/CP-03: terminal con Tomcat en 8080 y el navegador mostrando el portal.
- CP-04: pestaña Network sin 404 (CSS, JS, imagen, CDN de Bootstrap).
- CP-05: navbar, modal abierto y formulario funcionando.
- CP-06: respuesta de `/actuator/health` con `{"status":"UP"}`.
- CP-07: línea del log con el mensaje de `StartupReporter`.
- CP-08: resumen `BUILD SUCCESS` de `mvnw test`.
- CP-09: tamaño del JAR y el portal servido al ejecutar `java -jar`.
- CP-10/CP-11: el mismo JAR escuchando en 8081 con el perfil, y de vuelta en 8080 sin él.
- CP-12: salida de `git status` sin `target/` ni archivos de IDE.
